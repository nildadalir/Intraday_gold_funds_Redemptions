---
name: extract-configs-to-yaml
description: Extracts hardcoded configuration values from original Python code and suggests additions to config.yaml. Use when migrating Python extract projects, consolidating parameters, identifying hardcoded configs, or when asked to update config.yaml from original code.
disable-model-invocation: true
---

# Extract Configs to YAML

Project copy of the personal skill. Prefer the personal skill for cross-repo work; keep this file for teammates cloning the repo.

Use this skill when consolidating hardcoded parameters into `config.yaml`.

Start in **Plan mode**. Do not write files until the developer approves.

Full workflow: see personal skill `extract-configs-to-yaml` or ask the agent to follow the extract-configs-to-yaml skill steps (read config.yaml → scan original code → suggest → wait → update plan → execute).
